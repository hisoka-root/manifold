plugins {
    id 'net.neoforged.moddev'
}

base {
    archivesName = 'manifold-compat-ae2'
}

neoForge {
    version = neoforge_version

    mods {
        manifold_compat_ae2 {
            sourceSet sourceSets.main
        }
    }
}

dependencies {
    implementation project(':manifold-api')
}
